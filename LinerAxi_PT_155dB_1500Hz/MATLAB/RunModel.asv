%% Run the parametric model choosing the start stop and frequency step

 freq=2000

    LinerAxi_PT_155dB_parametric_v5_2(freq) %Run the model and extract results
    DataPath=['..\Data\Results_' num2str(freq) 'Hz.mat'];
    eval(['data=load(DataPath)'])
    
    for cycles=4:10
        ImpedanceCalc(4,freq,load(DataPath))
    end
